using Newtonsoft.Json.Linq;
using System.Reflection;

namespace MatchMake.Backend.Setup
{
    //public abstract class ConfigModule<T> : Autofac.Module where T : class
    //{
    //    protected readonly T Config;

    //    public const string fileNameConfig = "appsettings.json";

    //    protected ConfigModule()
    //    {
    //        Config = JObject.Parse(File.ReadAllText(Path.Combine(Path.GetDirectoryName(Assembly.GetEntryAssembly().Location),"appsettings.json"))).SelectToken("settings").ToObject<T>();
    //    }
    //}

}
